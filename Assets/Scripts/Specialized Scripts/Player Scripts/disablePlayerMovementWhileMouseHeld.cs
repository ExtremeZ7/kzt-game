using UnityEngine;
using System.Collections;

public class disablePlayerMovementWhileMouseHeld : MonoBehaviour {

	/*private PlayerControl pc;

	void Start () {
		pc = FindObjectOfType<PlayerControl>();
	}

	void Update () {
		if (Input.GetMouseButtonDown (0)) {
			pc.canMove = false;
		}
		if (Input.GetMouseButtonUp(0)){
			pc.canMove = true;
		}
	}*/
}
