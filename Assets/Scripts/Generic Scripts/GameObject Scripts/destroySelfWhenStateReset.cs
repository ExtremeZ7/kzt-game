using UnityEngine;
using System.Collections;

public class destroySelfWhenStateReset : MonoBehaviour {
	//This script actually doesn't need any code but still needs to be present in the Object for it to be any use. 

	//Look up LevelManager.cs.
}
